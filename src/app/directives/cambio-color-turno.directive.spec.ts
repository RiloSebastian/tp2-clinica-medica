import { CambioColorTurnoDirective } from './cambio-color-turno.directive';

describe('CambioColorTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new CambioColorTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
